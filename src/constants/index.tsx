export enum columnTypes {
  NUMBER = 'number',
  CURRENCY = 'currency',
  TOTALNUNMER = 'totalNumber',
  TOTALCURRENCY = 'totalCurrency',
  DATE = 'date',
}

export const swrConfig: any = {
  // throwOnError: false,
  revalidateIfStale: false,
  revalidateOnFocus: false,
  revalidateOnReconnect: false,
}
